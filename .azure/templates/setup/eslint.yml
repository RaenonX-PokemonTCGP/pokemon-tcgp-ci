steps:
- task: Cache@2
  inputs:
    key: 'eslint | "$(Agent.OS)" | "$(Build.SourceBranch)" | yarn.lock'
    restoreKeys: |
      eslint | "$(Agent.OS)" | "$(Build.SourceBranch)"
      eslint | "$(Agent.OS)"
      eslint
    path: $(CACHE_DIR_ESLINT)
  displayName: Cache ESLint Results
